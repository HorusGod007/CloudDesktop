# /etc/fail2ban/filter.d/clouddesktop.conf
# Fail2ban filter for CloudDesktop authentication failures

[Definition]
failregex = AUTH_FAILURE:.*ip=<HOST>
ignoreregex =

# /etc/fail2ban/jail.d/clouddesktop.conf
# [clouddesktop]
# enabled  = true
# port     = http,https
# filter   = clouddesktop
# logpath  = /var/log/clouddesktop/web.log
# maxretry = 5
# bantime  = 3600
# findtime = 600
